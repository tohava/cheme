(decl ((func int int) putchar))
(decl ((func double double) sqrt))


(var
 ((fun int unit) putint (lambda (z)
						  (if (== z 0)
							  (putchar 48)
							  (while (> z 0)
									 (begin
									   (putchar (+ (mod z 10) 48))
									   (= z (div z 10)))))))
 
 (int x 1))
(while (< x 1000)
	   (var (int y (double2int (sqrt (int2double x)))))
	   (while (and (> y 1) (> (mod x y) 0))
			  (= y (- y 1)))
	   (if (== y 1) 
		   (begin (putint x) (putchar 10) unitv))
	   (= x (+ x 1)))
